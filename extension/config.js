// Shared configuration, loaded before background.js and content.js.
//
// Replace BACKEND_BASE_URL with your deployed Worker URL (see server/README
// or the top-level README) before loading the extension.
const BACKEND_BASE_URL = "https://REPLACE_ME.workers.dev";
